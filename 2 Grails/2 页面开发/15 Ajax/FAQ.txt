. 1. 某个页面， 开始打开的时候是没有数据传递过来的， 通过提交表单后，后台查询结果后， 将结果集发送给该页面， 该页面才会以Ajax 方式显示查询结果。 

     那么问题来了， 该页面为了能处理查询结果， 必然有相关的Javascript脚本， 但是第一次打开页面的时候， 是没有数据转送过来的， 意味着有关参数可能是空的， 一旦涉及到类似JSON 的操作， 就会出现在空对象上的操作， 会提示错误。。。。


render就是渲染的意思。
而渲染，就是这样一个过程，浏览器根据页面的html代码、css定义、javascript脚本的操作，在浏览器上按照一定的规范(传说中的 DOCTYPE )显示出相应的内容。
举个最简单的例子：

<b>这个是粗体显示</b>

浏览器根据上面的代码在浏览器上面显示出粗体的文字，这个过程就叫渲染。